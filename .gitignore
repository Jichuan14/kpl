# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.production
# Deployment credentials are provisioned on the server and must never be committed.
deploy/.htpasswd

# Data / local DB
backend/data/*.db
backend/data/*.sqlite*
*.db
*.sqlite
*.sqlite3
*.jsonl
analysis/outputs/*/draft_model.json
*.json
*.pt
!analysis/hero_ability_mechanics.json
!analysis/hero_draft_feature_vectors.json
!analysis/hero_tactical_roles.json
# These are versioned RAG corpus inputs, not generated runtime data.
!knowledge/metadata/tencent-patch-index.json
!knowledge/sources/official/herolist.json
*.gz
# Hero artwork under frontend/public/assets/heroes is a deployable source asset.
# Do not ignore it: a clean checkout must include the icons used by heroAssets.js.

# IDE / OS
.idea/
.vscode/
.DS_Store
*.log

# Node (frontend later)
node_modules/
dist/
.npm

# Locally generated deployment archives; upload these directly to servers.
releases/
